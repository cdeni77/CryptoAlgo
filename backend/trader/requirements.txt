# Crypto Trading System - Data Collection Pipeline
# Python 3.9+

# Core async HTTP
aiohttp>=3.8.0

# Crypto exchange integration

# Data processing
pandas>=2.0.0
numpy>=1.24.0
coinbase-advanced-py

# Utilities

# Testing
pytest>=7.0.0
# Parallel test execution; pytest.ini sets `-n auto`, so this is required
# rather than optional — without it pytest exits on an unrecognised argument.
pytest-xdist>=3.5.0

# Development
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Training
scikit-learn
lightgbm
scipy
joblib

# Optional calibration backend (used when calibration method is set to beta)

# Optional Redis-backed queue (in-memory queue remains default)
redis>=5.0.0

# Research store (core/datastore.py). Imported at module scope by every research
# script via scripts/_common.py, so the container cannot start without them.
duckdb>=1.0.0
pyarrow>=15.0.0

# Kalshi authentication. RSA-PSS over SHA-256, not an HMAC secret, so this is a
# hard requirement rather than a transitive one — `data_collection/kalshi_client`
# imports it at signing time and the container would fail on the first live
# cycle without it.
cryptography>=42.0.0

# Postgres bridge (for pg_writer.py)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
