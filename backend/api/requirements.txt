fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic==2.9.2
coinbase-advanced-py

# Needed when running /trader optimization/training scripts from the backend container
numpy>=1.24.0
pandas>=2.0.0
scikit-learn
lightgbm
aiohttp
scipy
joblib

# The research store. `POST /research/launch` runs `python -m scripts.<module>`
# with THIS container's interpreter, and every trader script reaches
# core/datastore.py through scripts/_common.py, which imports duckdb at module
# scope. Without these the dashboard's script runner dies on import — the same
# defect the trader's own requirements once had.
duckdb>=1.0.0
pyarrow>=15.0.0

# Optional Redis-backed queue for trader data pipeline
redis>=5.0.0

# Tests. httpx backs fastapi.testclient.
pytest>=7.0.0
httpx>=0.27.0
